/**
 * Calculate the absolute difference between two numbers
 * @ignore
 * @param {number} numA
 * @param {number} numB
 * @return {number}
 */
export default function length(numA: number, numB: number) {
    return Math.abs(numA - numB);
}